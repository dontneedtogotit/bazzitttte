# ZRAM swap for the NUC7i5BNH - the i5-7260U is only 2C/4T so we trade a
# little CPU for a much larger effective swap space backed by RAM instead of
# the SSD. Capped at 4GiB regardless of RAM size.
[zram0]
zram-size = min(ram / 2, 4096)
compression-algorithm = lz4
