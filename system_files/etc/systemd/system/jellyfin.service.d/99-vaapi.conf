# bazzitttte: give the Jellyfin service access to the Intel GPU so VAAPI
# hardware transcoding works (Iris Plus 640 -> /dev/dri/renderD128).
# The render/video groups own the DRI render nodes; adding them as
# supplementary groups avoids having to usermod the jellyfin user.
[Service]
SupplementaryGroups=render video
